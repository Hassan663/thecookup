import 'package:flutter/material.dart';
class OpenRoom extends StatelessWidget {
  const OpenRoom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
